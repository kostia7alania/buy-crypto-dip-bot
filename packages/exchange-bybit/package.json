{
  "name": "@buy-crypto-dip-bot/exchange-bybit",
  "private": true,
  "type": "module",
  "exports": {
    "types": "./src/index.ts",
    "import": "./dist/index.mjs",
    "default": "./dist/index.mjs"
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --out-dir dist --no-dts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@buy-crypto-dip-bot/exchange-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "tsdown": "catalog:",
    "@types/node": "catalog:"
  }
}
