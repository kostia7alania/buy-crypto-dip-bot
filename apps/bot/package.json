{
  "name": "@buy-crypto-dip-bot/bot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --import tsx --env-file=../../.env --watch src/index.ts",
    "build": "tsdown src/index.ts --format esm --out-dir dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "grammy": "catalog:",
    "@buy-crypto-dip-bot/shared-types": "workspace:*",
    "@buy-crypto-dip-bot/db": "workspace:*",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "tsdown": "catalog:"
  }
}
