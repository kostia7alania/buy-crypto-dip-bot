# CODEX.md

Read `AGENTS.md` first.

Use the same rules as Jules and Claude.

Do not edit root config files unless the task explicitly asks for an infra change.

Before finishing:

```bash
pnpm typecheck
pnpm lint
pnpm test
```
